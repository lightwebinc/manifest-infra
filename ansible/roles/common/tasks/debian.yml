---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install build dependencies (Ubuntu/Debian)
  ansible.builtin.apt:
    name:
      - acl
      - build-essential
      - git
      - curl
      - ca-certificates
      - tar
    state: present
