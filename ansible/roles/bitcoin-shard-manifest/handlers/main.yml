---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  when: ansible_os_family != 'FreeBSD'

- name: Restart bitcoin-shard-manifest
  ansible.builtin.service:
    name: "{{ 'bitcoin-shard-manifest' if ansible_os_family != 'FreeBSD' else 'bitcoin_shard_manifest' }}"
    state: restarted
